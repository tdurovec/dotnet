<Window x:Class="BookEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookEditor" xmlns:library="clr-namespace:BookRecords.Library;assembly=BookRecords.CommonLibrary" d:DataContext="{d:DesignInstance Type=library:BookCollection}"
        mc:Ignorable="d"
        Title="BookEditor" Height="600" Width="650">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" HorizontalAlignment="Left" Margin="10, 0, 0, 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Content="Open..." Grid.Column="0" Height="25" Width="80" Click="Open_Click"/>
            <Button Content="Save as..." Grid.Column="1" Margin="5, 0, 0, 0" Height="25" Width="80" Click="SaveAs_Click"/>
        </Grid>

        <Grid Grid.Row="1" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>

            <ListBox x:Name="Books" Grid.Column="0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="{Binding Author}" FontStyle="Italic" Margin="0,0,5,0" />
                            <TextBlock Text="{Binding Title}" FontWeight="Bold" />
                            <TextBlock Foreground="Gray">
                                <Run Text="Publisher: " />
                                <Run Text="{Binding Publisher}" />
                                <Run Text="  Year: " />
                                <Run Text="{Binding Year}" />
                                <Run Text="  ISBN: " />
                                <Run Text="{Binding Isbn}" />
                            </TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" VerticalAlignment="Top" Margin="5">
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition />
                    </Grid.RowDefinitions>

                    <Button Content="Add..." Grid.Row="0" Height="25" Click="AddBook_Click"/>
                    <Button Content="Remove" Grid.Row="1" Height="25" Margin="0, 5, 0, 0" Click="Remove_Click"/>
                    
                </Grid>

                <Grid Grid.Row="1" VerticalAlignment="Bottom" Margin="5">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding MojeKnihy.Count, UpdateSourceTrigger=PropertyChanged, StringFormat='Count: {0}'}" />
                    </StackPanel>
                </Grid>
            </Grid>
            
        </Grid>

    </Grid>
</Window>
